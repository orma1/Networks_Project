# Server Configuration
# --------------------

# Probability of dropping a data packet (0.0 to 1.0)
data_loss_rate: 0.05

# Probability of dropping an ACK packet (0.0 to 1.0)
ack_loss_rate: 0.02

# Simulated network latency in milliseconds
latency_ms: 50